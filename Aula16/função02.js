function soma(n1 = 0, n2 = 0){ // deixa pré-definido como zero, pois caso não seja passado um valor pra algum parametro durante a chamada, ele adota o zero.
    return n1 + n2
}

console.log(soma(2, 5))
console.log(soma(4))
